services:
  app:
    build:
      context: .
      target: dev
    image: noahhumbert:dev
      - .env.dev
    container_name: dev
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/noahhumbert.com