<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href=" {{ path('home') }} ">
            <img class="img-fluid" src=" {{ asset('images/logo.png') }} " alt="Banner Hero" style="width: 50px; height: 50px;">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="nav-link active fw-medium" aria-current="page" href=" {{ path('projects') }} ">Projects</a>
        </div>
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a class="btn btn-primary align-self-end" href=" {{ path('login') }} " role="button">Sign In</a>
            </div>
        {% elseif is_granted('ROLE_ADMIN') %}
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a class="btn btn-primary align-self-end" href=" {{ path('dashboard') }} " role="button">Dashboard</a>
                <a class="btn btn-primary align-self-end" href=" {{ path('admin') }} " role="button">Admin</a>
            </div>
        {% else %}
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a class="btn btn-primary align-self-end" href=" {{ path('dashboard') }} " role="button">Dashboard</a>
            </div>
        {% endif %}
    </div>
</nav>