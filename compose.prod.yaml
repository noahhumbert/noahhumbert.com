version: '3.9'

services:
  app:
    build:
      context: .
      target: prod
    container_name: symfony_app_prod
    ports:
      - "80:80"
    volumes: []  # does not change the container code as local changes are made
